# Skill Usage (Optional)

This repo includes an optional Claude Code skill configuration:

- `/.claude/skills/multi-ai-planner.md`

Purpose: route tasks to Codex (backend) and Gemini (frontend) first, and treat Claude as integration/docs only, to reduce Claude usage.

Notes:
- This repository does **not** include a standalone `multi-ai-planner.js` CLI tool.
- Canonical project doc remains `CLAUDE.md`.

